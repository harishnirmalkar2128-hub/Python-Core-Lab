# 1. Using a Built-in Module
import math
import random

print("Square root of 16 is:", math.sqrt(16))
print("Random number between 1-10:", random.randint(1, 10))

# 2. External Module (Pehle install karna padega)
# Terminal mein type karo: pip install requests

import requests # Iska use website se data lene ke liye hota hai
# response = requests.get("https://google.com")
# print("Status Code:", response.status_code)